# 《摊·URBAN ECHO》比赛功能清单、技术说明与录制指南

版本：比赛录屏版

适用环境：iOS 16.0 及以上；当前主要验证环境为 iOS 26.5 Simulator

产品定位：通过 AI 建档、地图探索、记忆导览与社区补档，记录流动摊贩、老手艺人与城市街巷记忆。

> 《摊》不是外卖、购物或预约平台。价格仅作为档案信息展示，不提供下单、支付、购物车或配送能力。

## 一、功能清单

### 1. 双角色入口

| 功能 | 用户端 | 摊户端 |
| --- | --- | --- |
| 本地身份登录 | 支持 | 支持 |
| 角色并排选择 | 支持 | 支持 |
| 登录默认入口 | 发现页 | AI 建档页 |
| 登录后切换身份 | 切换后进入地图 | 切换后进入地图 |
| 数据持久化 | 本地保存 | 本地保存 |

当前登录仅用于区分单设备上的演示身份，不代表已经接入真实账号系统。

### 2. 用户端

#### 发现城市档案

- 浏览附近摊位、老手艺和街巷档案。
- 按类别、关键词和档案状态筛选。
- 查看档案名称、人物、类别、年限、参考价格和社区状态。
- 将档案加入或移出守护清单。
- Seed 档案首次安装即可展示封面、故事和社区内容。

#### 地图探索

- 使用 Apple MapKit 显示地图。
- 使用 Core Location 获取用户位置和处理授权状态。
- 展示摊位地理坐标标记。
- 搜索并聚焦指定档案。
- 查看档案当前地点和历史活动地点。
- 成都档案范围内使用真实起点规划路线。
- 用户异地或定位不可用时，经提示后使用成都演示起点。

#### 昔涟城市记忆导览

- 通过 `MKDirections` 请求步行路线。
- 使用 `MKRoute` 和 `MKPolyline` 绘制路线。
- 昔涟以地图 Annotation 的经纬度坐标移动。
- 按路线累计距离插值，避免因坐标点疏密产生速度突变。
- 路线生成后只调整一次主要可视区域，不持续抢夺用户的缩放和平移。
- 用户缩放、平移或旋转地图时，昔涟仍保持在路线地理坐标上。
- 路线请求失败时使用方向线兜底，并标注“非真实步行路线”。
- 到达后昔涟停留在档案坐标，展示一次到达介绍卡。
- 支持继续进入当前档案上下文的昔涟问答。

#### 档案详情

- 档案封面、名称、摊主或手艺人信息。
- 街巷故事、制作过程、传承线索。
- 常驻地点和历史活动范围。
- 档案影像及图片说明。
- 评论、照片补档和状态确认。
- 当前用户可删除自己提交的社区内容。
- 状态线索明确提示：

> 信息来自社区线索，不代表摊主实时营业承诺。

#### 社区共建

- 发布纯文字评论。
- 发布文字加图片记录。
- 发布仅图片补档，并可补充说明。
- 单条评论最多附带 3 张图片。
- 图片支持现场拍照和从相册选择。
- 发布“仍在”“暂未见到”“位置有变化”等状态线索。
- 状态线索可附现场照片和文字说明。
- 发布成功后计入补档贡献和到访记录。

#### 我的

- 守护清单。
- 到访记录。
- 补档贡献。
- 点亮记录。
- 用户身份与角色切换入口。

### 3. 摊户端

#### AI 辅助建档

- 拍照或从相册选择 1 张档案封面。
- 使用系统键盘输入自然语言介绍。
- 使用普通话、成都话或自贡话入口录入口述。
- 实时显示语音转写结果。
- 转写结果可继续使用系统键盘修改。
- 用户主动发送后，DashScope AI 将文字整理为结构化档案。
- 生成摊位名称、摊主、类别、年限、参考价格、故事摘要和制作工序。
- AI 输出异常时生成可继续编辑的本地草稿，App 不崩溃。
- 用户确认后保存档案并回到地图查看。

照片当前作为档案封面和补充材料保存，AI 建档主要依据用户文字或语音转写内容，不宣称已经完成图片视觉识别。

#### 真实语音转文字

- 底层使用 DashScope `fun-asr-realtime`。
- 普通话、成都话、自贡话使用同一个实时识别模型。
- 成都话和自贡话入口明确标注“使用四川话识别能力”，不宣称为不同模型。
- 点按开始，再次点按停止。
- 最长录音时间为 60 秒。
- 页面退出或 App 进入后台时释放麦克风。
- 网络失败时保留已有输入和最后一段有效转写。
- 不自动发送给 AI，不保存原始录音。
- 麦克风权限拒绝后仍可继续打字建档。

#### 档案维护

- 查看自己建立的档案。
- 编辑档案文字、制作过程和展示指标。
- 使用中文图标选项编辑指标，不显示 SF Symbol 技术名称。
- 更新档案封面。
- 查看社区为该档案留下的评论、影像和状态线索。

## 二、比赛 Seed 数据

首次安装统一加载 5 个档案：

1. 张大爷糖油果子；
2. 李爷爷三大炮；
3. 青羊宫油炸面点摊；
4. 蜀绣小铺；
5. 老赵补鞋换拉链。

每个档案均包含：

- 档案封面；
- 摊主或手艺人姓名；
- 类别和从业年限；
- 参考价格；
- 故事摘要；
- 制作工序；
- 经纬度；
- 当前档案状态；
- 历史活动地点；
- 2 条社区评论。

“张大爷糖油果子”作为主演示档案，额外包含：

- 3 张档案影像；
- 图片说明；
- 2 条社区评论；
- 1 条带图片的社区状态线索；
- 完整历史活动路线；
- 昔涟到达介绍；
- 昔涟上下文问答内容。

Seed 人名按比赛档案化名处理。外部真实图片应继续保留来源记录，并以“同类手艺影像资料”等准确措辞展示，不把资料图片伪装成档案人物现场照片。

## 三、技术文档

### 1. 技术栈

| 层级 | 实现 |
| --- | --- |
| UI | SwiftUI |
| 地图 | Apple MapKit、`MKMapView`、`MKDirections`、`MKPolyline` |
| 定位 | Core Location |
| 相机 | `UIImagePickerController` + `UIViewControllerRepresentable` |
| 相册 | PhotosUI `PhotosPicker` |
| 状态管理 | `ObservableObject`、`@Published`、`@EnvironmentObject` |
| 文本 AI | DashScope Qwen |
| 实时 ASR | DashScope `fun-asr-realtime` WebSocket |
| 本地数据 | Codable JSON |
| 本地图片 | Application Support 私有目录 |
| 最低系统 | iOS 16.0 |

### 2. 分层结构

#### 界面层

- `RootView`：登录、双角色和主 Tab 结构。
- `DiscoverView`：发现、筛选和档案入口。
- `ArchiveMapView`：地图、路线、昔涟和地图搜索。
- `ArchiveDetailView`：档案信息、影像和社区共建。
- `AIArchiveBuilderView`：语音或文字输入、AI 整理和档案确认。
- `ProfileView`：用户贡献或摊户档案维护概览。

#### 状态与业务层

- `ArchiveStore` 是主要状态容器。
- 页面通过 `ArchiveStore` 读取档案、角色、Tab、守护、到访和贡献状态。
- 评论、状态确认和建档保存统一从 Store 进入，不由 View 直接写 JSON。

#### 数据边界

```swift
protocol ArchiveRepository {
    func loadSnapshot() async throws -> ArchiveSnapshot
    func saveSnapshot(_ snapshot: ArchiveSnapshot) async throws
}

protocol PhotoStorageService {
    func saveImage(_ image: UIImage, caption: String?) async throws -> PhotoAttachment
    func loadImage(_ attachment: PhotoAttachment, thumbnail: Bool) async -> UIImage?
    func deletePhoto(_ attachment: PhotoAttachment) async throws
}
```

当前实现：

- `LocalArchiveRepository`
- `LocalPhotoStorageService`

未来可替换为：

- 远程数据库 Repository；
- OSS、CloudKit、Firebase Storage 或其他对象存储；
- 服务端 AI 密钥代理。

UI 不需要知道数据来自 JSON、本地文件还是远程服务器。

### 3. 本地持久化

- 完整档案快照保存为 Application Support 中的 JSON。
- 用户图片保存至 Application Support 的应用私有图片目录。
- 图片主文件长边限制约 2048 像素。
- 同时生成详情图和缩略图。
- Codable 模型只保存相对文件名，不保存 `UIImage`、临时 URL 或 `PhotosPickerItem`。
- 重启 App 后从 JSON 和相对文件名恢复。
- 删除用户记录时同步清理其主图和缩略图。
- Bundle Seed 图片不会被用户删除操作清理。

### 4. 地图与昔涟

地图显示层使用 `UIViewRepresentable` 封装 `MKMapView`，由 `MKMapViewDelegate` 管理：

- 用户位置 Annotation；
- 摊位 Annotation；
- 昔涟 Annotation；
- 路线 Overlay；
- 路线 Renderer；
- 地图可视区域。

昔涟位置只由经纬度驱动。路线坐标会建立累计距离数组，动画根据目标距离计算所在路线段并插值坐标。动画任务在重新规划、再次导览或页面退出时取消，复用同一个昔涟 Annotation，只更新其 `coordinate`。

### 5. AI 与语音

#### QwenArchiveAgent

输入：

- 用户自然语言讲述；
- 已转写的语音文字；
- 当前建档上下文。

输出：

- JSON 结构化档案草稿。

异常处理：

- 20 秒网络超时；
- 防止重复点击；
- JSON 清理与解析；
- 解析失败后生成可修改的本地草稿；
- 已输入文字和图片不会因请求失败而丢失。

#### XilianChatAgent

- 将当前档案作为问答上下文。
- DashScope 请求成功时显示真实回答。
- 网络失败时根据当前档案生成本地上下文回答。
- 请求期间显示加载状态并阻止重复发送。

#### SpeechRecognitionService

- 通过协议隔离语音识别实现。
- 使用 `URLSessionWebSocketTask` 连接 DashScope。
- 麦克风音频转换为 16kHz、单声道 PCM。
- 处理部分结果、最终结果、停止、取消和失败事件。
- 不使用 Apple Speech，因此只申请麦克风权限。
- 不保存原始音频，不输出 API Key。

### 6. 权限

当前使用：

- `NSLocationWhenInUseUsageDescription`
- `NSCameraUsageDescription`
- `NSPhotoLibraryUsageDescription`
- `NSMicrophoneUsageDescription`

权限拒绝后的原则：

- App 不崩溃；
- 显示清晰说明；
- 必要时提供“前往设置”；
- 保留已填写文字和已选内容；
- 地图在无定位时仍能查看档案；
- 语音不可用时仍能使用系统键盘。

### 7. 真实能力与规划边界

当前已经实现：

- 本地 JSON 和本地图片持久化；
- 真实 DashScope 文本请求；
- 真实 DashScope 实时语音转文字；
- Apple MapKit 路线规划；
- 昔涟沿坐标路线移动；
- 评论、照片补档和状态线索；
- 双角色演示闭环。

当前没有实现：

- 真实用户认证；
- 云端数据库；
- 跨设备同步；
- OSS 图片上传；
- 多用户实时社区；
- 内容审核、举报和冲突处理；
- 服务端 AI 密钥代理；
- 外卖、下单、支付、购物车或预约。

比赛中应表述为：

> 当前版本采用本地 Repository 与图片服务完成单设备稳定演示，并保留远程实现替换边界。跨设备社区共建、对象存储、用户认证和 AI 密钥代理属于后续规划。

## 四、录制建议

### 1. 推荐录制设备

- 主画面：iPhone 17、iOS 26.5 Simulator。
- 语音识别：优先使用真机；如果使用模拟器，应先确认 Mac 麦克风能够传入模拟器。
- 画面方向：竖屏。
- 外观：建议使用浅色模式作为主视频；夜间模式可作为补充能力快速展示。
- 动态字体：默认大小。
- 网络：稳定 Wi-Fi，录制前分别测试一次 Qwen 和 Fun-ASR。
- 地图位置：设置为成都预设起点附近，避免异地提示中断主演示。

### 2. 首次安装镜头与录屏就绪态

建议分两步准备：

1. 当前首次安装状态用于录制 App 图标、启动和双角色登录入口。
2. 完成定位、相机、相册和麦克风授权后，使用 Debug 演示重置恢复统一 Seed。

这样正式流程仍是干净数据，但不会被多次系统权限弹窗打断。

正式录制前确认：

- 5 个 Seed 档案存在；
- 每个档案的 2 条评论存在；
- 张大爷 3 张档案影像和状态照片正常；
- 到访、点亮和新增档案已重置；
- DashScope Key 可用；
- 地图能够返回路线；
- 麦克风输入可用；
- 通知、电话和即时通讯软件已关闭。

### 3. 推荐主视频结构

建议总时长控制在 3 分 30 秒至 4 分钟。

| 时间 | 画面 | 建议口播 |
| --- | --- | --- |
| 00:00–00:12 | App 图标、启动页、双角色入口 | “《摊》记录流动摊贩、老手艺人与城市街巷记忆。” |
| 00:12–00:35 | 用户端登录、发现页浏览 | “用户可以从附近档案和消失预警中发现容易被忽略的城市记忆。” |
| 00:35–01:00 | 张大爷档案详情 | “档案不仅记录地点，还保存人物、手艺过程、影像和社区线索。” |
| 01:00–01:35 | 地图、路线生成、昔涟移动 | “昔涟是城市记忆向导，会沿 Apple MapKit 规划的步行路线陪伴用户前往。” |
| 01:35–01:55 | 到达卡、昔涟上下文问答 | “到达后，昔涟讲述当前档案故事，并继续回答与摊位相关的问题。” |
| 01:55–02:25 | 发布照片补档和状态线索 | “用户可以上传现场影像、补充故事，并留下不等同于营业承诺的社区状态线索。” |
| 02:25–02:38 | 切换摊户端 | “另一端帮助不熟悉复杂表单的摊主和手艺人建立自己的档案。” |
| 02:38–03:10 | 方言口述、实时转写、键盘修改 | “摊主可以用普通话或四川话口述，识别结果仍由本人修改和确认。” |
| 03:10–03:35 | 真实 AI 整理、确认保存 | “DashScope 将自然语言整理为结构化档案，照片作为封面和资料保存。” |
| 03:35–03:50 | 地图查看新档案或重启恢复 | “档案保存后可重新被城市发现，并在重启后继续保留。” |
| 03:50–04:00 | Logo 和四个核心亮点 | “AI 建档、地图发现、昔涟导览、社区共建，让城市记忆被看见。” |

### 4. 方言录制文案

建议提前练习一段 10–15 秒的自然口述，不要临场发挥过长内容。例如：

> “我这个摊叫周师傅油炸面点，在青羊宫附近摆了二十多年。每天早上现和面、现下锅，一份大概六块钱。希望把这个手艺和以前摆摊的地方都记录下来。”

录制时：

- 先展示“成都话”或“自贡话”入口上的四川话能力说明；
- 点按开始，正常语速说完；
- 点按停止；
- 展示实时转写；
- 用系统键盘修改一个识别词；
- 最后再主动发送给 AI。

这能同时证明识别是真实输入，并体现“AI 整理、用户确认”的产品原则。

### 5. 录制操作原则

- 每次点击后停顿约 0.5–1 秒，让评委看清反馈。
- 地图路线出现后不要频繁缩放，保留一次主动缩放来证明昔涟没有偏离路线。
- AI 请求期间保留加载画面 1–2 秒，不必完整等待很久。
- 不快速扫过长页面，重点展示标题、故事、图片、时间和状态说明。
- 不展示源码、控制台、API Key、Debug 菜单或模拟器设置过程。
- 不把方向线兜底称为真实步行路线。
- 不把资料图称为档案人物现场照片。
- 不把文字 AI 建档称为图片自动识别。
- 不在视频中承诺已上线后端或跨设备社区。

### 6. 失败预案

#### AI 请求失败

- 停止本次录制，不在主成片中展示失败。
- 检查网络和本地 DashScope 配置。
- 重新发起测试后再录。
- 保留 App 的本地草稿降级作为稳定性能力，但不要把降级草稿冒充真实网络结果。

#### 实时语音识别效果不理想

- 缩短口述；
- 减少环境噪声；
- 使用清晰但自然的四川口音；
- 避免一次说多个陌生专有名词；
- 展示用户用键盘修正，而不是反复录制。

#### MapKit 路线失败

- 检查网络和模拟定位；
- 确认起点位于成都档案范围；
- 重新进入档案并规划；
- 若必须展示兜底方向线，画面中保留“非真实步行路线”标签。

### 7. 成片建议

- 录制至少 2 个完整版本。
- 保留一份未经压缩的原始视频。
- 剪辑以真实连续操作为主，只压缩网络等待时间。
- 使用简短字幕标出四个亮点：
  - AI 辅助建档；
  - 地图发现；
  - 昔涟城市记忆导览；
  - 社区共同补档。
- 背景音乐音量应明显低于口播，方言识别片段最好暂时降低音乐。
- 结尾显示产品定位，不展示交易、商业闭环或尚未实现的技术承诺。

## 五、录制前最终检查表

- [ ] App 从统一 Seed 状态启动。
- [ ] 登录账号和密码已提前确定。
- [ ] 用户端默认进入发现页。
- [ ] 切换角色后进入地图，摊户端登录默认进入 AI 建档。
- [ ] 张大爷档案封面、评论、影像和状态线索完整。
- [ ] 地图定位位于成都附近。
- [ ] 路线能够生成。
- [ ] 昔涟沿路线移动并只显示一次到达卡。
- [ ] 昔涟中文输入和中文回答正常。
- [ ] 拍照与相册入口可用。
- [ ] 普通话和四川话实时转写可用。
- [ ] 系统键盘可以修改转写文本。
- [ ] AI 建档真实请求成功。
- [ ] 新档案保存后能够查看。
- [ ] 重启 App 后档案和图片仍存在。
- [ ] 最后一条社区内容不被 Tab Bar 遮挡。
- [ ] 视频中没有 API Key、控制台、Mock 或 Debug 信息。
- [ ] 视频中没有购买、订单、支付或购物车入口。
- [ ] 已准备第二份完整备用录制。
